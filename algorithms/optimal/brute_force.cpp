#include <algorithm.h>

class BruteForce: BinPackingAlgorithm {
public:
    string name() const override {
        return "BruteForce";
    }

    Packing pack(const vector<Item> &items) override {
        
    }
};